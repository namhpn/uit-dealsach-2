# Repo Current State

Last updated: 2026-05-26

## Current Branch

`chore/t0001-dockerized-dev-tooling`

Baseline source branch before ticket work: T0000 was reviewed as the previous captured baseline state.

## Completed Tickets

| Ticket | Date | Summary |
|---|---:|---|
| T0000 | 2026-05-26 | Captured baseline folder structure, scripts, dependency state, and build/test results without changing application behavior. |
| T0001 | 2026-05-26 | Normalized Docker Compose commands, added Dockerized frontend npm service, installed frontend npm dependencies, and fixed the default backend PHPUnit baseline without runtime feature changes. |

## Current Folder Structure

Top-level folders and key project areas observed:

```text
.
├── .codex/
│   └── skills/
├── backend/
│   ├── app/
│   ├── public/
│   ├── tests/
│   ├── vendor/
│   ├── writable/
│   └── phpunit.xml
├── database/
├── docker/
│   └── nginx/
├── docs/
│   ├── frontend/
│   └── templates/
└── frontend/
    ├── node_modules/        # local install output, untracked
    ├── package-lock.json
    └── src/
```

Important root files:

```text
.dockerignore
AGENTS.md
Dockerfile
composer.phar
docker-compose.yml
```

## Tech Stack

Frontend:

* React
* TypeScript
* Vite
* Tailwind CSS
* Radix UI primitives
* Lucide React
* Recharts
* npm lockfile generated by Dockerized `npm install`

Backend:

* CodeIgniter 4 app starter
* PHP 8.2
* PHPUnit 10
* MariaDB through Docker Compose

Runtime:

* Docker Compose
* PHP-FPM
* Nginx
* MariaDB
* phpMyAdmin
* Dockerized Node 20 frontend command service

## Installed Dependencies

### Frontend

Observed state after T0001:

* `frontend/package.json` exists.
* `frontend/package-lock.json` exists and is kept because T0001 standardizes on npm commands.
* `frontend/node_modules/` exists locally after `docker compose run --rm frontend npm install`.
* `frontend/node_modules/` is untracked and must not be committed.
* `frontend/pnpm-workspace.yaml` still exists from the baseline, but there is no pnpm lockfile and the required T0001 npm install/build commands pass.

### Backend

Observed state after T0001:

* `backend/composer.json` exists.
* `backend/composer.lock` exists.
* `backend/vendor/` exists through the Docker image and local mounted volume.
* `backend/.env` exists for CodeIgniter runtime configuration.
* `backend/phpunit.xml` exists for normal local PHPUnit runs without requiring a coverage driver.

## Available Scripts / Commands

### Frontend

Declared in `frontend/package.json`:

```bash
docker compose run --rm frontend npm install
docker compose run --rm frontend npm run build
docker compose run --rm --service-ports frontend npm run dev -- --host 0.0.0.0
```

The frontend Compose service bind-mounts the repository and does not use a named or anonymous volume for `frontend/node_modules/`.

### Backend

Declared in `backend/composer.json`:

```bash
composer test
```

Documented Docker commands:

```bash
docker compose run --rm app sh -lc 'cd backend && php ../composer.phar install'
docker compose run --rm app sh -lc 'cd backend && php spark list'
docker compose run --rm app sh -lc 'cd backend && php spark migrate'
docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit'
docker compose up -d --build
docker compose down
```

### Docker Services

`docker compose config --services` reports:

```text
db
app
frontend
nginx
phpmyadmin
```

Compose variable substitution no longer requires `--env-file backend/.env`. The `app` service still loads `backend/.env` through Compose `env_file` so CodeIgniter can use project runtime configuration, while Docker service defaults use safe non-secret development values.

## Build/Test Status

| Area | Command | Last Result | Notes |
|---|---|---|---|
| Docker | `docker compose config` | Passed | Runs without `--env-file backend/.env`. |
| Docker | `docker compose up -d --build` | Passed | App image built and services started. |
| Backend | `docker compose run --rm app sh -lc 'cd backend && php spark list'` | Passed | CodeIgniter v4.7.3 command list loads successfully in Docker. |
| Backend | `docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit'` | Passed | 5 tests and 7 assertions pass; command exits 0 using `backend/phpunit.xml`. |
| Frontend | `docker compose run --rm frontend npm install` | Passed with audit warning | Installed 285 packages, generated `frontend/package-lock.json`, and reported 1 high-severity npm audit issue. |
| Frontend | `docker compose run --rm frontend npm run build` | Passed with chunk-size warning | Vite production build completed; Vite warned one chunk is larger than 500 kB. |
| Docker | `docker compose down` | Passed | Stack stopped cleanly after verification. |

## Manual Verification Performed

1. Reviewed required project docs and ticket scope.
2. Verified `docker compose config` succeeds without `--env-file backend/.env`.
3. Verified `docker compose config --services` includes `frontend`.
4. Verified the frontend service bind-mounts the repository and does not define a `frontend/node_modules` volume.
5. Ran `docker compose run --rm frontend npm install`.
6. Confirmed `frontend/node_modules/` exists locally.
7. Ran `docker compose run --rm frontend npm run build`.
8. Ran `docker compose up -d --build`.
9. Ran `docker compose run --rm app sh -lc 'cd backend && php spark list'`.
10. Ran `docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit'`.
11. Confirmed `AGENTS.md` no longer contains Docker commands with `--env-file backend/.env`.
12. Removed generated `frontend/dist/` output after build verification so it is not left as a ticket change.
13. Ran `docker compose down` and confirmed the stack stopped cleanly.

Browser/UI verification was not performed because T0001 is a developer tooling baseline ticket and does not change runtime UI implementation.

## Known Issues

See `docs/Known_Issues_And_Followups.md`.

Open items after T0001:

* KI-0004 — npm reports 1 high-severity audit issue after frontend install.
* KI-0005 — frontend generated-output ignore rules are not present in repo `.gitignore`; `.gitignore` was outside T0001 allowed areas.

Closed in T0001:

* KI-0001 — frontend dependencies installed and Dockerized frontend build passes.
* KI-0002 — default backend PHPUnit exits 0 without requiring a coverage driver.
* KI-0003 — `AGENTS.md` template path and Docker command references corrected.

## Next Recommended Ticket

T0002 — Address frontend dependency audit and generated-output ignore rules, with `.gitignore` and dependency files explicitly in scope.
