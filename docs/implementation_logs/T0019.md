# T0019 — Commerce-First Neubrutalist Homepage Refresh

## Original Ticket

This section preserves the original ticket contract verbatim for T0019.

## Goal

Implement the clarified DealSach homepage refresh as a commerce-first, API-backed, controlled Neubrutalist discovery page using the existing public catalog systems, with small discovery metadata/API enhancements and no homepage admin system.

## Dependencies

* T0018 completed and reviewed.
* Existing public discovery API:

  * `GET /api/public/discovery`
  * `GET /api/public/filters`
  * `GET /api/public/books`
  * `GET /api/public/books/suggestions`
* Existing frontend homepage:

  * `frontend/src/app/pages/HomePage.tsx`
  * `frontend/src/app/shared.tsx`
  * `frontend/src/app/api.ts`
* Existing public catalog backend:

  * `backend/app/Libraries/PublicCatalogService.php`
  * `backend/app/Controllers/PublicCatalogController.php`
  * `backend/tests/feature/PublicCatalogApiTest.php`
* Existing seeded demo data:

  * `backend/app/Database/Seeds/DealSachDemoSeeder.php`
* Current design/decision context:

  * Homepage is shopping/discovery-first, brand second.
  * Use controlled Neubrutalism.
  * Keep three current API discovery sections.
  * Keep hero banners frontend-configured.
  * Do not add homepage admin CRUD.
  * Card CTA text may say `Đến nơi bán →`, but it means navigating to `/book/{id}`, not external seller redirect.

## Allowed Areas

Codex may edit only:

* `frontend/src/app/api.ts`
* `frontend/src/app/shared.tsx`
* `frontend/src/app/pages/HomePage.tsx`
* `frontend/src/app/Root.tsx`
* `frontend/src/app/pages/SearchPage.tsx`
* `frontend/src/app/pages/ProductDetailPage.tsx`
* `frontend/src/app/pages/WishlistPage.tsx`
* `frontend/src/app/pages/AlertsPage.tsx`
* `frontend/src/app/pages/AccountPage.tsx`
* `backend/app/Libraries/PublicCatalogService.php`
* `backend/app/Controllers/PublicCatalogController.php`
* `backend/app/Database/Seeds/DealSachDemoSeeder.php`
* `backend/tests/feature/PublicCatalogApiTest.php`
* `docs/Manual_Verification_Guide.md`
* `docs/implementation_logs/T0019.md`

Always allowed for ticket bookkeeping:

* `docs/Repo_Current_State.md`
* `docs/Known_Issues_And_Followups.md`

## Do Not Touch

Codex must not edit:

* `docker-compose.yml`
* `Dockerfile`
* `docker/php-entrypoint.sh`
* `backend/app/Config/Email.php`
* `backend/app/Config/Routes.php`, unless a test proves a route registration change is strictly required.
* Auth/session logic.
* Wishlist persistence schema.
* Price alert persistence schema.
* Admin CRUD pages or Admin APIs, except not at all for this ticket.
* Database migrations, unless implementation proves impossible without one.
* Composer or npm dependency files.
* Buy-flow redirect behavior.
* SMTP behavior.
* Existing autocomplete behavior except where shared types require safe compatibility.

## Requirements

### Homepage product direction

* The homepage must prioritize shopping/discovery over brand-only presentation.
* The visual style must remain controlled Neubrutalist:

  * sharp corners,
  * black borders,
  * hard shadows,
  * flat colors,
  * Be Vietnam Pro,
  * no gradients,
  * no soft shadows,
  * no rounded cards/buttons/inputs.
* Keep the existing homepage section order unless the existing code structure requires a minor ordering adjustment:

  1. Hero banner carousel.
  2. Featured books grouped by category.
  3. Recent price drops.
  4. Popular clicked deals.
  5. How it works.

### Color-token cleanup

* Refactor frontend color tokens so red is no longer named `secondary`.
* Add a true `secondary` token with `#496173`.
* Add a red commercial/error token such as `priceAlert` or `dealRed` using `#ba1a1a`.
* Update existing frontend usages intentionally:

  * price text and price-drop badges use `priceAlert`/`dealRed`;
  * actual secondary styling uses `secondary`;
  * error states may use `priceAlert`/`error`.
* Preserve existing visual intent where possible.

### Hero banner behavior

* Keep hero banners frontend-configured.
* Do not add a backend/admin banner system.
* Keep the CTA behavior “as is” in principle: CTA should navigate into the existing public flow rather than require a new backend feature.
* Add enough config structure so each banner can have explicit behavior without new APIs:

  * link to `/search`,
  * link to a section anchor,
  * or navigate to an existing route.
* Do not add direct external seller redirect from hero banners.

### Discovery API metadata

* Extend discovery section payloads with small metadata only:

  * `subtitle`
  * `cta_label`
  * `cta_href`
  * optional `window` metadata for time-windowed sections.
* Keep the existing three discovery sections:

  * `featured_books`
  * `recent_price_drops`
  * `popular_clicked_deals`
* Do not replace the discovery response with a generic dynamic `sections[]` architecture in this ticket.
* Do not add new search sorts such as `price_drop_7d` or `popular_7d`.

### Category shelves

* Keep featured books grouped by category.
* Use category `display_label` with fallback to `name`.
* Render category `display_description` where available.
* Continue using current `is_featured` and category ordering.
* Do not add per-category featured ordering.
* Do not add homepage-specific category admin controls.

### Product cards

* Homepage product cards must link to `/book/{id}`.
* Product card CTA text should be `Đến nơi bán →`, but it must navigate to `/book/{id}`.
* Do not implement direct seller redirect from homepage cards.
* Do not add `best_buy_action` to cards.
* Wishlist buttons must remain on homepage cards and continue opening auth dialog for unauthenticated users.
* Do not batch wishlist status in this ticket.
* Do not add price-alert creation controls to homepage cards.

### Product card content

* Compact cards should show:

  * cover/fallback,
  * category,
  * title,
  * author,
  * current eligible price or public status label,
  * offer count,
  * wishlist action.
* Larger deal cards should additionally show publisher if space allows.
* Product cards should not show individual price disclaimers.
* Use a section-level or module-level disclaimer instead.
* If there is no eligible current price, show the public status label.

### Original/reference price behavior

* Add API support for “original price” display where possible using the highest current eligible price across all reseller offers for the same book.
* Do not invent MSRP or fake original price.
* Add a field with clear semantics, for example:

  * `highest_eligible_price`
  * or `reference_highest_eligible_price`
* Display strikethrough/reference price only when:

  * `lowest_eligible_price` is not null,
  * the highest eligible price is not null,
  * and highest eligible price is greater than lowest eligible price.
* For recent price-drop cards, continue showing VND savings from `price_drop.amount`.
* Do not add discount percentage.

### Seed/test data

* Ensure seeded demo data contains enough eligible multi-retailer price variation to display highest-vs-lowest reference price on at least some homepage cards.
* Preserve existing deterministic test expectations where possible.
* Update tests if public card DTO shape changes.

### Empty states and fallbacks

* Keep existing empty-state behavior for:

  * no featured books,
  * no recent price drops,
  * no popular clicked deals.
* Add clear section subtitles/empty messages that explain:

  * recent price drops are based on eligible price observations in the recent window;
  * popular deals are based on successful affiliate redirects in the recent window.
* Do not hide a section silently just because it is empty.

### Accessibility and UX

* Preserve keyboard navigation and focus visibility.
* Preserve autocomplete header behavior from T0018.
* Do not regress search submission behavior.
* Carousel arrows must remain accessible with `aria-label`.
* Book cover fallback must remain functional.

## Non-Goals

* Do not implement homepage banner database tables.
* Do not implement homepage section admin CRUD.
* Do not add direct seller redirect from homepage cards.
* Do not add `best_buy_action`.
* Do not add new search sorts.
* Do not redesign SearchPage, ProductDetailPage, WishlistPage, AlertsPage, or AccountPage beyond token compatibility.
* Do not fix KI-0009 demo cover assets.
* Do not fix KI-0014 Docker container-name conflict.
* Do not change SMTP, auth, wishlist, price-alert, or buy-flow persistence behavior.
* Do not add new dependencies.

## Acceptance Criteria

* [ ] Homepage remains API-backed and renders existing three discovery sections.
* [ ] Hero banners remain frontend-configured and do not require backend/admin banner storage.
* [ ] Hero CTA behavior remains compatible with existing public routes/anchors.
* [ ] Discovery API returns section metadata:

  * `subtitle`,
  * `cta_label`,
  * `cta_href`,
  * optional `window` where relevant.
* [ ] Frontend `DiscoveryResponse` types match the backend response.
* [ ] Featured category shelves render `display_description` where present.
* [ ] Homepage cards no longer render individual price disclaimers.
* [ ] A section/module-level price disclaimer remains visible.
* [ ] Product card CTA text is `Đến nơi bán →` and navigates to `/book/{id}`.
* [ ] Homepage cards do not directly redirect to `/go/offers/{offerId}`.
* [ ] Larger deal cards show publisher when layout space allows.
* [ ] Book card DTO includes a clearly named highest-current-eligible-price/reference-price field.
* [ ] Reference/strikethrough price displays only when highest eligible price is greater than lowest eligible price.
* [ ] Recent price-drop cards keep showing VND savings from `price_drop.amount`.
* [ ] No discount percentage is added.
* [ ] Frontend color tokens separate true `secondary` from red price/error accent.
* [ ] Existing autocomplete behavior remains intact.
* [ ] Existing wishlist behavior on cards remains intact.
* [ ] Backend public catalog tests pass.
* [ ] Full backend PHPUnit suite passes.
* [ ] Frontend build passes.
* [ ] Docs/state files are updated.

## Manual Verification

1. Run backend focused tests:

   ```bash
   docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit --filter PublicCatalog'
   ```

2. Expected result:

   ```txt
   OK
   ```

3. Run full backend tests:

   ```bash
   docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit'
   ```

4. Expected result:

   ```txt
   OK
   ```

5. Run frontend build:

   ```bash
   docker compose run --rm frontend npm run build
   ```

6. Expected result:

   ```txt
   build succeeds
   ```

7. Start the normal development stack if local ports/container names allow:

   ```bash
   docker compose up -d --build
   ```

8. Open the homepage.

9. Expected result:

   * Hero banner renders with controlled Neubrutalist styling.
   * Featured category shelves render.
   * Category descriptions appear where seeded metadata exists.
   * Recent price drops render with VND savings.
   * Popular clicked deals render when seeded redirect data exists.
   * Cards show current price or status.
   * Cards show `Đến nơi bán →`.
   * Clicking a card CTA opens `/book/{id}`, not an external seller URL.
   * Wishlist button still opens auth dialog when logged out.
   * No individual card-level price disclaimer appears.
   * A section/module-level price disclaimer remains visible.

10. Verify discovery response shape:

```bash
docker compose run --rm app sh -lc 'cd backend && php spark routes | grep "api/public/discovery"'
```

11. Expected result:

* Public discovery route still exists.

12. Optional API smoke check:

```bash
curl -s http://localhost/api/public/discovery
```

13. Expected result:

* JSON includes `featured_books`, `recent_price_drops`, `popular_clicked_deals`.
* Each section includes metadata fields added by this ticket.
* Book items include the new highest/reference eligible price field.

14. Verify T0018 autocomplete regression:

* Type a Vietnamese or unaccented query in the header search box.
* Expected result:

  * Suggestions load asynchronously.
  * Suggestion click opens `/book/{id}`.
  * Pressing Enter still navigates to `/search?...`.

## Completion Report Required

Codex must report:

* Summary of changes.
* Files changed.
* Commands run.
* Build/test results.
* Manual verification steps performed.
* Risks.
* Follow-up tickets.
* Docs updated.

---

# T0019 Completion Report

## Summary

Implemented T0019 in allowed areas only:

* Refreshed homepage discovery UX as commerce-first Neubrutalism while preserving API-backed section order.
* Kept hero banners frontend-configured and added explicit CTA behavior config (`search`, `anchor`, `route`) without backend/admin banner CRUD.
* Added discovery metadata fields (`subtitle`, `cta_label`, `cta_href`, optional `window`) for the existing three sections without introducing dynamic `sections[]` architecture.
* Added `highest_eligible_price` to public book card DTOs using current eligible offers, enabling optional reference/strikethrough price display when `highest > lowest`.
* Updated homepage cards to keep wishlist behavior, use `Đến nơi bán →` for homepage CTA text, and navigate to `/book/{id}` (not `/go/offers/{offerId}`).
* Moved homepage to section-level price disclaimer usage (no per-card disclaimer on homepage cards).
* Split color tokens so `secondary` is neutral (`#496173`) and red commercial/error usage moves to `dealRed` (`#ba1a1a`).
* Updated seeded observation timing to avoid future-dated “today” observations and kept deterministic seed scenario coverage.

## Files Changed

* `backend/app/Libraries/PublicCatalogService.php` — discovery metadata additions and `highest_eligible_price` card field.
* `backend/app/Database/Seeds/DealSachDemoSeeder.php` — seed-time stability fix for same-day freshness + eligible price variation.
* `backend/tests/feature/PublicCatalogApiTest.php` — assertions for new discovery metadata and highest-price field behavior.
* `frontend/src/app/api.ts` — discovery and book-card DTO updates.
* `frontend/src/app/shared.tsx` — token cleanup, homepage card/reference-price behavior, category shelf description rendering, and section metadata rendering.
* `frontend/src/app/pages/HomePage.tsx` — explicit banner CTA action handling, section anchors, discovery metadata wiring, and module-level disclaimer placement.
* `frontend/src/app/Root.tsx` — token compatibility updates for header suggestion accents/errors.
* `frontend/src/app/pages/ProductDetailPage.tsx` — token compatibility updates only.
* `frontend/src/app/pages/WishlistPage.tsx` — token compatibility updates only.
* `frontend/src/app/pages/AlertsPage.tsx` — token compatibility updates only.
* `frontend/src/app/pages/AccountPage.tsx` — token compatibility updates only.
* `docs/Manual_Verification_Guide.md` — added reusable Homepage Discovery Refresh verification section.
* `docs/Repo_Current_State.md` — updated state for T0019.
* `docs/implementation_logs/T0019.md` — added this completion report.

## Commands Run

```bash
docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit --filter PublicCatalog'
docker compose run --rm app sh -lc 'cd backend && php vendor/bin/phpunit'
docker compose run --rm frontend npm run build
docker compose run --rm app sh -lc 'cd backend && php spark routes | grep "api/public/discovery"'
```

Result highlights:

* PublicCatalog suite passed: `17 tests, 143 assertions`.
* Full backend suite passed: `78 tests, 778 assertions`.
* Frontend build passed (existing Vite chunk-size warning remains).
* Discovery route registration check passed.

## Build/Test Results

| Check | Result | Notes |
|---|---|---|
| Backend PublicCatalog subset | Pass | `17 tests, 143 assertions`. |
| Backend full PHPUnit | Pass | `78 tests, 778 assertions`. |
| Frontend build | Pass | Vite build succeeded; existing chunk warning unchanged. |
| Route registration check | Pass | `GET /api/public/discovery` confirmed. |

## Manual Verification

1. Ran required backend focused tests, full backend tests, frontend build, and discovery-route check.
2. Verified seeded-time regression root cause for stale classification (future-dated same-day observations) and fixed it by seeding observations relative to current Vietnam time.
3. Did not run browser screenshot automation in this ticket run; manual interactive homepage confirmation remains recommended before merge if visual proof is required.

## Risks

* Manual browser visual verification for final homepage styling/interaction was not captured in this run.
* Existing Vite chunk-size warning remains unchanged from prior tickets.

## Follow-up Tickets

* KI-0014 remains: remove fixed Docker `container_name` usage to improve disposable-stack reliability.
* KI-0009 remains: add real demo cover assets or align seed paths.

## Docs Updated

* [x] `docs/Repo_Current_State.md`
* [ ] `docs/Known_Issues_And_Followups.md` (no update needed)
* [x] `docs/Manual_Verification_Guide.md`
* [x] `docs/implementation_logs/T0019.md`
